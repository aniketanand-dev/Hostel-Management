const express = require('express');
const router = express.Router()
const { testNw, addUserTest, getUserTestNw } = require('./../controllers/test.controller');

//router.post('/testNw', testNw);

//router.post('/user', addUserTest)
//router.get('/user', getUserTestNw)

module.exports = router